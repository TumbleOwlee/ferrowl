# Open Issues

1. Floating box over commandline whenever commandline is in focus to display supported commands
2. Support changing register type, e.g. `HoldingRegister` to `Coil`
    * Requires better `EditInputDialog` and `EditSelectionDialog` since coils/discrete inputs only support boolean
3. Support value and raw value for virtual registers (currently no value is shown, no editing is possible)
